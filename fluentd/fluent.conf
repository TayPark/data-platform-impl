<source>
    @type forward
    port 24224
    bind 0.0.0.0
</source>

<match docker.**>
    @type copy
    
    <store>
        @type stdout
    </store>
    
    <store>
        @type file
        path /fluentd/log
        <format>
            @type json
        </format>
    </store>
</match>
